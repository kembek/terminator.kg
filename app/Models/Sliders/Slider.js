'use strict'

const Model = use('Model')
/**
 * @swagger
 * definitions:
 *   Slider:
 *     type: object
 *     properties:
 *       title:
 *         type: string
 *       description:
 *         type: string
 *       link:
 *         type: string
 *       img:
 *         type: string
 */
class Slider extends Model {
}

module.exports = Slider
